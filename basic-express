//SERVER
app.listen(3000,function(){ 
    console.log("Server Has Started");
})

//ADDING BODY PARSER (first run "npm i body-parser --save" in terminal)
var bodyParser = require('body-parser')
app.use(bodyParser.urlencoded({ extended: false }))


